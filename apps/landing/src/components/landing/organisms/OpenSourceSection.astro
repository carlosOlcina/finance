---
import ActionLink from '../atoms/ActionLink.astro';
import Eyebrow from '../atoms/Eyebrow.astro';
import NoteCard from '../molecules/NoteCard.astro';

interface Props {
  repoUrl: string;
  releasesUrl: string;
  notes: string[];
}

const { repoUrl, releasesUrl, notes } = Astro.props;
---

<section class="mx-auto max-w-6xl px-6 py-10 md:py-16">
  <div class="rounded-[2.5rem] bg-[var(--text-100)] px-6 py-8 text-[var(--surface-900)] shadow-[0_28px_80px_rgba(51,36,20,0.16)] md:px-8 md:py-10">
    <div class="grid gap-8 lg:grid-cols-[0.95fr_1.05fr] lg:items-end">
      <div>
        <Eyebrow text="Open source / distribucion" tone="inverse" />
        <h2 class="font-display mt-4 text-4xl font-semibold leading-tight md:text-5xl">El proyecto quiere vivir a plena vista: codigo abierto, instalacion clara y cero humo.</h2>
        <p class="mt-4 max-w-2xl text-sm leading-7 text-[rgba(255,250,242,0.78)] md:text-base">
          Finance nacio para resolver un problema cotidiano de forma directa. El repositorio publico sera el centro de documentacion y colaboracion, y la instalacion en Android se apoyara en el APK publicado en GitHub.
        </p>
      </div>

      <div>
        <div class="space-y-3">
          {notes.map((note) => (
            <NoteCard text={note} />
          ))}
        </div>

        <div class="mt-6 flex flex-wrap gap-3">
          <ActionLink href={repoUrl} label="Ver el repositorio" variant="dark" external />
          <ActionLink href={releasesUrl} label="Ver releases" variant="dark-outline" external />
        </div>
      </div>
    </div>
  </div>
</section>